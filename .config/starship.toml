add_newline = false
format = "$all$fill$time$cmd_duration$status$line_break$character"

[fill]
symbol = ' '

[time]
disabled = false
time_format = '%T, %d/%m/%Y'

[directory]
truncation_length = 0
format = "[$path/]($style)[$read_only]($read_only_style) "

